/*
 * LOGIC BRIEFING:
 * Login — Login page stub (secret route — not in navigation)
 * Placeholder until login-page feature is implemented.
 * Accessible only by typing /#/login directly in the browser.
 * No Supabase calls. No state. Static placeholder only.
 */

function Login() {
  return (
    <div className="page-stub">
      {/* SECTION: STUB CONTENT */}
      <h1>Login</h1>
      <p>Login page — auth form coming in the login-page feature.</p>
    </div>
  )
}

export default Login
